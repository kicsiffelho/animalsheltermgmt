<Window x:Class="AnimalShelterMgmt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:AnimalShelterMgmt.Views"
        xmlns:viewmodels="clr-namespace:AnimalShelterMgmt.ViewModels"
        Title="Állatmenhely" Height="1080" Width="1920"
        WindowState="Maximized" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewmodels:MainWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="396" />
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Column="0" Content="{Binding CurrentView}" />

        <Grid Grid.Column="1" Background="White" Margin="20,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,40,0,0">
                <Button Content="Home" Margin="10,10,10,15" FontWeight="Bold" Foreground="DarkRed" Command="{Binding ShowHomeCommand}" />
                <Button Content="Profile" Margin="10,15,10,10" Command="{Binding ShowProfileCommand}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <Button Content="Animals" Margin="10,10,10,15" Command="{Binding ShowAnimalsCommand}" />
                <Button Content="New animal" Margin="10,15,10,10" Command="{Binding ShowNewAnimalCommand}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <Button Content="Login" Margin="10,15,10,15" Command="{Binding ShowLoginCommand}" />
                <Button Content="Registration" Margin="10,15,10,0" Command="{Binding ShowRegistrationCommand}"/>
                <Button Content="About" Margin="10,100,10,10" Command="{Binding ShowAboutCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>