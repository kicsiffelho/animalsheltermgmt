<UserControl x:Class="AnimalShelterMgmt.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AnimalShelterMgmt.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="350">
            <TextBlock Text="Bejelentkezés" FontSize="32" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Felhasználónév:" />
            <TextBox Text="{Binding Username}" Margin="0,0,0,10" />

            <TextBlock Text="Jelszó:" />
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20"/>

            <Button Content="Bejelentkezés"
                    Command="{Binding LoginCommand}"
                    Width="150" HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       Margin="0,20,0,0"
                       TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>