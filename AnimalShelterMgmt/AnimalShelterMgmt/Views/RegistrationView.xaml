<UserControl x:Class="AnimalShelterMgmt.Views.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AnimalShelterMgmt.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:RegistrationViewModel />
    </UserControl.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="350">
            <TextBlock Text="Regisztráció" FontSize="32" FontWeight="Bold" Margin="0,0,0,20" TextAlignment="Center"/>

            <TextBlock Text="Felhasználónév:"/>
            <TextBox Text="{Binding Username}" Margin="0,0,0,10"/>

            <TextBlock Text="E-mail cím:"/>
            <TextBox Text="{Binding Email}" Margin="0,0,0,10"/>

            <TextBlock Text="Jelszó:"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>

            <TextBlock Text="Jelszó megerősítése:"/>
            <PasswordBox x:Name="ConfirmBox" Margin="0,0,0,20"/>

            <Button Content="Regisztráció"
                    Width="150"
                    HorizontalAlignment="Center"
                    Command="{Binding RegistrationCommand}"/>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       Margin="0,20,0,0"
                       TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
